version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./backend/prisma:/app/backend/prisma
    restart: unless-stopped
